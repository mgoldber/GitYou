import React from 'react';

const Header = (props) => {
    return (
        <header className="Header__Component">
            <form>
                
            </form>
        </header>
    )
}

export default Header;